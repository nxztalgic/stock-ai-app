<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sentiment Analysis</title>
  <link href="style.css" rel="stylesheet">
  <style>
    body {
      background-image: url('https://img.freepik.com/premium-vector/business-candle-stick-graph-chart-stock-market-investment_41981-866.jpg?semt=ais_items_boosted&w=740');
      background-size: cover;
      background-position: center;
      background-attachment: fixed;
      color: #fff;
    }
    .section {
      background-color: rgba(0, 0, 0, 0.6);
      max-width: 800px;
      margin: 3rem auto;
      padding: 2rem;
      border-radius: 12px;
      box-shadow: 0 0 10px rgba(255, 255, 255, 0.1);
    }
    input, button {
      padding: 0.5rem;
      margin-top: 0.5rem;
      margin-bottom: 1rem;
      border-radius: 5px;
      border: none;
    }
    button {
      background-color: #1f2937;
      color: white;
      cursor: pointer;
    }
    button:hover {
      background-color: #374151;
    }
  </style>
</head>
<body>
  <div class="section">
    <h1>💬 Sentiment Analysis</h1>
    <p>
      Our Sentiment Analysis engine uses state-of-the-art Natural Language Processing (NLP) models from <strong>Hugging Face</strong>.
      It analyzes real-time financial news, tweets, and corporate announcements to gauge public opinion and market sentiment around a company.
    </p>

    <form id="sentimentForm">
      <label for="company">Enter Company Name or Keyword:</label><br>
      <input type="text" id="company" placeholder="e.g., Tesla, Apple" required />
      <br><button type="submit">Analyze</button>
    </form>

    <div>
      <h2>Sample Insights:</h2>
      <ul>
        <li>📈 Tesla sentiment: Positive – driven by innovation and EV demand.</li>
        <li>📉 Meta sentiment: Negative – impacted by privacy concerns.</li>
        <li>⚖ Amazon sentiment: Mixed – balancing layoffs and cloud growth.</li>
      </ul>
    </div>

    <div id="result"></div>
  </div>

  <script>
    document.getElementById("sentimentForm").addEventListener("submit", async function(e) {
      e.preventDefault();
      const company = document.getElementById("company").value;

      const response = await fetch("https://api-inference.huggingface.co/models/distilbert-base-uncased-finetuned-sst-2-english", {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
          "Authorization": "Bearer YOUR_HUGGINGFACE_API_TOKEN"
        },
        body: JSON.stringify({ inputs: company })
      });

      const result = await response.json();
      const label = result?.[0]?.label || "Unknown";
      document.getElementById("result").innerText = `Sentiment Result: ${label}`;
    });
  </script>
</body>
</html>