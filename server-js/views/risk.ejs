<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Risk Assumed Score</title>
  <link href="style.css" rel="stylesheet">
  <style>
    body {
      background-image: url('https://img.freepik.com/premium-vector/business-candle-stick-graph-chart-stock-market-investment_41981-866.jpg?semt=ais_items_boosted&w=740');
      background-size: cover;
      background-position: center;
      background-attachment: fixed;
      color: #fff;
    }
    .section {
      background-color: rgba(0, 0, 0, 0.6);
      max-width: 800px;
      margin: 3rem auto;
      padding: 2rem;
      border-radius: 12px;
      box-shadow: 0 0 10px rgba(255, 255, 255, 0.1);
    }
    input, button {
      padding: 0.5rem;
      margin-top: 0.5rem;
      margin-bottom: 1rem;
      border-radius: 5px;
      border: none;
    }
    button {
      background-color: #1f2937;
      color: white;
      cursor: pointer;
    }
    button:hover {
      background-color: #374151;
    }
  </style>
</head>
<body>
  <div class="section">
    <h1>⚠ Risk Assumed Score</h1>
    <p>
      Our Risk Assumed Score tool evaluates a stock's volatility based on historical performance,
      sector sensitivity, and current market behavior. This helps investors gauge the potential
      downside risk before making investment decisions.
    </p>

    <form id="riskForm">
      <label for="stock">Enter Stock Name or Symbol:</label><br>
      <input type="text" id="stock" placeholder="e.g., TSLA, INFY" required />
      <br><button type="submit">Calculate Risk</button>
    </form>

    <div>
      <h2>Example Assessments:</h2>
      <ul>
        <li>🔥 Tesla: High Risk – high volatility in EV sector.</li>
        <li>🏦 HDFC Bank: Low Risk – stable performance in BFSI sector.</li>
        <li>🛒 Amazon: Medium Risk – fluctuating global retail trends.</li>
      </ul>
    </div>

    <div id="riskResult"></div>
  </div>

  <script>
    document.getElementById("riskForm").addEventListener("submit", function(e) {
      e.preventDefault();
      const stock = document.getElementById("stock").value;
      const score = Math.random(); // Placeholder logic
      let riskLevel = "Moderate";
      if (score > 0.7) riskLevel = "High";
      else if (score < 0.3) riskLevel = "Low";
      document.getElementById("riskResult").innerText = `Estimated Risk Score for ${stock}: ${riskLevel}`;
    });
  </script>
</body>
</html>